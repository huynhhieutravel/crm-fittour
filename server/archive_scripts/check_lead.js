const { Pool } = require('pg'); require('dotenv').config(); const pool = new Pool({ connectionString: process.env.DATABASE_URL }); pool.query("SELECT l.id, l.name, l.tour_id, c.external_id FROM leads l JOIN conversations c ON l.id = c.lead_id WHERE l.name = 'Viet Ha' OR l.name = 'Cuc Chau Chau'").then(res => { console.log(res.rows); process.exit(); }).catch(e => { console.error(e); process.exit(); })
