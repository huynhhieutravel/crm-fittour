const { Pool } = require('pg'); require('dotenv').config(); const pool = new Pool({ connectionString: process.env.DATABASE_URL }); pool.query("SELECT id FROM tour_templates WHERE is_active = true AND keywords IS NOT NULL AND keywords != ''").then(res => { const idx313 = res.rows.findIndex(r => r.id === 313); const idx317 = res.rows.findIndex(r => r.id === 317); console.log('Index 313:', idx313, 'Index 317:', idx317); process.exit(); }).catch(e => { console.error(e); process.exit(); })
