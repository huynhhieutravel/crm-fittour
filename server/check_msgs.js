const { Pool } = require('pg'); require('dotenv').config(); const pool = new Pool({ connectionString: process.env.DATABASE_URL }); pool.query("SELECT content, sender_type FROM messages m JOIN conversations c ON m.conversation_id = c.id JOIN leads l ON c.lead_id = l.id WHERE l.name IN ('Viet Ha', 'Cuc Chau Chau') ORDER BY m.created_at ASC LIMIT 10").then(res => { console.log(res.rows); process.exit(); }).catch(e => { console.error(e); process.exit(); })
